# --- web stack ---
fastapi==0.115.6
uvicorn[standard]==0.30.6     # brings uvloop, httptools, websockets

# --- db ---
SQLAlchemy==2.0.36
alembic==1.13.3
psycopg[binary]==3.2.3        # drop psycopg2 & psycopg-binary duplicates

# --- http/utils ---
requests==2.32.3
httpx==0.27.2                 # pairs with httpcore>=1.0; don't pin httpcore/h11
python-dotenv==1.0.1
orjson>=3.10                  # optional, FastAPI JSON speed-up

# --- LLM / embeddings ---
langchain==0.3.12
langchain-openai==0.3.3       # keep LC packages on the same minor
faiss-cpu==1.8.0.post1
tiktoken==0.7.0
